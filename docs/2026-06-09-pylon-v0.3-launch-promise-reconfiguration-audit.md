# Pylon v0.3 Launch Promise Reconfiguration Audit

Date: 2026-06-09

Status: audit for the new standalone `OpenAgentsInc/pylon` repo after Pylon
was extracted out of the `openagents` repo.

## Source Material Read

Primary v0.3 TUI source docs from `openagents/docs/pylon/tui/`:

- `monorepo_reset_deprecation_plan.md`
- `opentui_refactor_audit.md`
- `previous_tui.md`
- `pylon_v0.3_proposal.md`

Related launch and porting source material:

- `autopilot-omega/docs/2026-06-08-chrisjuan-pylon-agentic-revenue-gap-audit.md`
- `openagents/docs/pylon/README.md`
- `openagents/docs/pylon/PYLON_PLAN.md`
- `openagents/docs/pylon/OPEN_SOURCE_CONTRIBUTOR_BOUNDARY.md`
- `openagents/docs/pylon/PYLON_ACCOUNT_LINKING_PROCESS.md`
- `openagents/docs/pylon/PYLON_SANDBOX_CONTRACT.md`
- `openagents/docs/benchmarks/2026-06-08-pylon-benchmark-worker-capability-envelope.md`
- `openagents/crates/pylon-core/src/lib.rs`
- `openagents/apps/deprecated/pylon/src/*`
- `openagents/apps/deprecated/pylon-tui/src/*`
- `openagents/packages/pylon-bootstrap/src/*`

## Executive Verdict

The new standalone Pylon repo is the correct home for Pylon v0.3, but it is
not yet launch-equivalent to the old public Pylon surface.

The current standalone repo contains:

- package identity `@openagentsinc/pylon` at `0.3.0`;
- Bun, Effect, and OpenTUI dependencies;
- one OpenTUI app in `src/index.ts`;
- a fullscreen observational dashboard skeleton;
- a mocked hardware telemetry loop;
- an MDK `agent-wallet balance` poller;
- a stub presence heartbeat loop;
- an OpenCode diagnostic/composer path.

It does not yet contain the launch-critical public Pylon behavior from
`openagents`:

- release/bootstrap/update semantics from `@openagentsinc/pylon`;
- Pylon home/config/identity persistence;
- NIP-98 account linking and refresh;
- public provider presence registration and heartbeat payloads;
- wallet readiness, payout-target admission, and send-readiness state;
- assignment polling, lease acceptance, progress, artifact/proof, closeout,
  stale lease handling, and receipt projection;
- `pylon-core` public-safe projection validation;
- benchmark/GEPA worker capability envelope support;
- Qwen/training capability and receipt mapping, now explicitly postponed until
  after the GEPA-first v0.3 launch path;
- local ledger, earnings, payouts, settlement records, and wallet history;
- sandbox execution profile contracts and receipt gates;
- macOS/Linux install smoke coverage beyond the current repo skeleton.

The launch promise set therefore has to be reconfigured from "the old
`openagents` Pylon launcher and deprecated Rust crates prove this" to "the new
standalone `OpenAgentsInc/pylon` repo owns the contributor node and must emit
the exact same public-safe evidence." Until that reconfiguration is complete,
public copy must keep saying v0.3 is an extracted Pylon rewrite in progress,
not a broadly live install-to-Bitcoin network.

## Boundary Change

### Old Assumption

The June 8 launch audit and related gates were written around:

- `@openagentsinc/pylon@0.2.5` as a bootstrap launcher inside
  `openagents/packages/pylon-bootstrap`;
- deprecated Rust implementation homes under `openagents/apps/deprecated/pylon`
  and `openagents/apps/deprecated/pylon-tui`;
- public contract helpers in `openagents/crates/pylon-core`;
- release and proof scripts in `openagents/scripts/pylon` and
  `openagents/scripts/release`.

### New Required Assumption

Pylon v0.3 must be treated as:

- standalone repo: `OpenAgentsInc/pylon`;
- runtime package: `@openagentsinc/pylon`;
- initial supported platforms: macOS and Linux only;
- implementation stack: Bun + Effect + OpenTUI;
- contributor-owned public supply connector;
- source of truth for local identity, telemetry, wallet readiness, assignment
  execution, artifact/proof closeout, and local public-safe receipts;
- not a private Cloud node, not Treasury, not buyer UX, not a hidden labor
  runtime, and not a browser wallet bridge.

## Launch Promise Reconfiguration Matrix

| Launch promise | Existing evidence path | Required v0.3 reconfiguration |
| --- | --- | --- |
| "New Pylon release" | `@openagentsinc/pylon@0.2.5` bootstrap launcher and openagents release tags. | Use `@openagentsinc/pylon@0.3.0` from this repo, published publicly with the `pylon` bin. The initial release target is macOS and Linux only. Omega/public copy must name v0.3 only after this repo owns those release artifacts and smoke evidence. |
| "One install turns a computer into Bitcoin" | Old launcher could register/heartbeat, set up MDK receive readiness, and request payout-target admission. | Port install/bootstrap, identity creation, MDK setup, registration, heartbeat, wallet readiness, assignment readiness, and receipt bundle generation into this repo. Dashboard green state must use v0.3 heartbeats, not stale v0.2 rows. |
| "Pylons online now / wallet-ready / assignment-ready" | Omega reads public Pylon stats from registration and heartbeat APIs. | v0.3 must continuously emit fresh provider presence, wallet readiness, assignment readiness, capability refs, and blocker refs. Stale fibers must degrade counters to zero. |
| "GEPA work runs on Pylons" | Probe/Cloud/Psionic have retained and canary evidence; old Pylon assignments were represented by public refs. | Port the Pylon worker loop: poll leases, accept idempotently, run bounded Probe/GEPA task, stream progress, upload artifact/proof refs, submit closeout, and handle stale/cancelled leases. |
| "Paid Pylon work settles Bitcoin" | Two small-sats GEPA assignments settled through Nexus/Omega/MDK local agent-wallet bridge. | v0.3 must expose separate states for wallet configured, receive-ready, send-ready, payout-target admitted, payable pending settlement, settlement recorded, and public receipt projected. Do not treat balance or receive readiness as send readiness. |
| "MDK handles wallet capacity" | MDK is used, but mnemonic-only restore had zero outbound capacity; hosted programmatic payout was disabled. | v0.3 must include MDK readiness classifiers and stable blocker refs. It must preserve original-wallet-home requirements or block restored-wallet send claims until MDK exposes repair/send readiness. |
| "Qwen 3.6 fine-tuning on people's devices" | Psionic has local/loopback Qwen/Pylon evidence and gate docs. | Postpone this claim. v0.3 launch work focuses on GEPA first; Qwen/training ports remain a later track after GEPA worker loop, closeout, import, and payment-mode gates are solid. |
| "Local inference revenue" | Old Pylon plan models provider inventory and Gemma-first capability envelopes. | v0.3 must replace mocked telemetry with real hardware/model inventory and advertise exact product/capability envelopes before inference is listed as sellable. |
| "Sandbox execution revenue" | Sandbox contract is ratified in `openagents/docs/pylon/PYLON_SANDBOX_CONTRACT.md`. | v0.3 must implement explicit sandbox profiles, default-deny policies, resource limits, artifact policy, and receipts before sandbox compute is advertised. |
| "Data/trace revenue" | Launch audit labels this planned/gated; no live sale market. | v0.3 may capture local traces only after redaction, consent, valuation, purchase, entitlement, and payout contracts exist. No raw prompts, repo content, provider payloads, wallet material, or private data in public refs. |
| "Forum tips / Site referrals / checkout revenue" | Forum/Site flows live mostly in Omega; Pylon is not the product authority. | v0.3 should only surface wallet/payout readiness and receipt refs that Omega/Site/Forum can consume. It must not claim creator settlement, referral payout, or hosted checkout authority itself. |
| "Subscription capacity marketplace" | ChatGPT/Codex account connection exists elsewhere; Venice/Claude are planned/blocked. | v0.3 must add provider-account schemas, secret-ref policy, metering, route policy, pricing, ToS boundaries, and settlement refs before any capacity is sellable. Current OpenCode diagnostic/composer code is not a capacity-market contract. |
| "Plugin/signature revenue" | Probe/Omega/Psionic model signatures, candidates, and marketplace gates exist. | v0.3 should consume exact package/signature refs and emit usage/attribution events only after package validation, runtime activation policy, metering, rev-share, and settlement gates exist. |
| "Agent-readable launch sheet / OpenAPI coverage" | Omega has AGENTS.md, manifest, OpenAPI, and copy gates. | Those documents must be updated to point v0.3 Pylon install, package, endpoints, states, and blocked/planned claims at this repo. Discovery docs must not grant dispatch, spend, settlement, or provider mutation authority. |

## Required Ports Into `OpenAgentsInc/pylon`

### 1. Release, Bootstrap, And Install Surface

Port or replace from `openagents/packages/pylon-bootstrap`:

- macOS and Linux target resolution;
- GitHub release tag policy;
- install-root/cache layout;
- update polling while the dashboard is open;
- source-build fallback policy, if still desired;
- `--register-openagents`;
- `--setup-mdk-wallet`;
- public-safe `--pylon-ref`, display name, resource mode, capability refs;
- JSON summary output;
- macOS and Linux smoke fixtures.

Package decision: v0.3 uses `@openagentsinc/pylon`, is public, and exposes the
`pylon` bin from this repo. Launch docs should use only
`@openagentsinc/pylon` for the v0.3 launch package.

### 2. Identity, Config, Home, And Persistence

Port from `openagents/apps/deprecated/pylon/src/lib.rs` and related modules:

- Pylon home path and config path handling;
- local identity generation and persisted node label/npub/public key;
- local ledger path and state files;
- lifecycle states: offline, online, paused, degraded, assignment-ready;
- startup defaults;
- process/service-manager friendly headless mode;
- JSON output for status and diagnostics.

The v0.3 Effect app should have typed services for config, identity, runtime
state, and persistence instead of global UI variables.

### 3. Public Boundary And Projection Validation

Port from `openagents/crates/pylon-core`:

- `openagents.pylon_core.v1` or a v0.3 successor schema;
- contributor public surface list;
- private Cloud dependency marker denial;
- forbidden public keys such as wallet seed, private key, preimage, bearer
  token, API key, private topology, capacity-pool secret, and internal
  accounting credential;
- public-safe identity, admin, availability, inventory, lifecycle, heartbeat,
  and receipt projections;
- validation tests that reject secret/private fields.

The new repo should own this contract directly or publish a TypeScript package
with equivalent schema validation. Do not require the private `cloud` repo for
contributor Pylon validation.

### 4. Presence, Registration, And Account Linking

Port from `openagents/packages/pylon-bootstrap` and
`openagents/docs/pylon/PYLON_ACCOUNT_LINKING_PROCESS.md`:

- `/api/pylons/register`;
- `/api/pylons/:pylonRef/heartbeat`;
- `/api/pylons/:pylonRef/wallet-readiness`;
- `/api/pylons/:pylonRef/payout-target-admission`;
- `/api/pylon-links/complete`;
- `/api/pylon-links/refresh`;
- NIP-98 signed proof generation for link and refresh;
- body-hash validation;
- stale snapshot refresh behavior;
- web-safe Codex/provider capability snapshots.

The current v0.3 `startPresenceHeartbeatLoop` only sleeps. It must become a
real signed heartbeat fiber with retry/backoff, stale-state degradation, and
redacted failure refs.

### 5. Wallet, MDK, LDK, Payout, And Ledger Surfaces

Port or intentionally replace from `openagents/apps/deprecated/pylon/src`:

- `wallet_runtime.rs`;
- `wallet_harness.rs`;
- `ledger.rs`;
- wallet status, balance, address, invoice, offer, pay, history, channels,
  liquidity, telemetry, backup, restore, lock, entropy, and lifecycle reports;
- local ledger and idempotent wallet history sync;
- payout target kinds: `bolt12_offer`, `bolt11_invoice`, `bip353_name`,
  `lnurl_pay`;
- exact rail and payment artifact used in receipts;
- failure receipts for runtime refusal, reserve checks, unavailable BIP353
  resolution, and send failure;
- receive-ready versus send-ready classifier;
- original wallet home versus mnemonic-only restore blocker.

MDK remains the current launch bridge. v0.3 should wrap it deliberately and
redact all raw wallet/payment material. Native LDK fallback work must stay
contributor-side and must not become Nexus/Treasury custody.

### 6. Assignment Worker Loop

Port or define anew:

- assignment lease polling;
- campaign and selection policy refs;
- idempotent accept;
- no duplicate assignment;
- paused/stale/offline/wrong-capability denials;
- progress event submission;
- artifact/proof metadata refs;
- closeout submission;
- accepted/rejected outcome states;
- timeout and stale lease handling;
- rollback/cleanup receipts.

This is the missing path for the full Pylon network flow. Without it, v0.3 is a
dashboard shell, not an earning node.

### 7. Probe/GEPA And Benchmark Worker Capability

Port from `openagents/docs/benchmarks/2026-06-08-pylon-benchmark-worker-capability-envelope.md`
and related `benchmark-cloud` contracts:

- benchmark runner support;
- Harbor/Terminal-Bench support;
- Probe runtime support;
- local model support;
- rollout/eval support;
- model-training support;
- CPU/RAM/disk/GPU capacity;
- wall-clock and cost budget refs;
- isolation profile;
- artifact upload support;
- proof/receipt support;
- assignment lease and closeout support;
- payout readiness as a separate flag.

Keep GEPA text/workflow optimization separate from neural model training.
Worker admission must not imply payout readiness.

### 8. Deferred Qwen / Training Capability And Receipts

Postpone this section until after the GEPA-first v0.3 launch path. When the
team returns to Qwen/training, port from `training_trn_mapping.rs`, Pylon
training docs, and Psionic contracts:

- training capability tier/profile;
- accelerator inventory;
- eligible work class and replica type labels;
- artifact bundle and signed access handling;
- worker/validator role claims;
- shard receipt validation;
- terminal artifact upload receipts;
- merge/eval/admission refs;
- quarantine and bad-shard blocker states;
- payment/settlement separation.

This is not required for the first v0.3 GEPA launch. It is required before any
future v0.3 public copy says "Qwen 3.6 is fine-tuning on people's devices."

### 9. Hardware Telemetry And Provider Inventory

Replace the v0.3 mock telemetry:

```text
State: ACTIVE_IN_WORK
Model: Gemma-4-9B-SFT
VRAM: 8.2 GB / 12.0 GB
```

with real platform discovery equivalent to the Rust `sysinfo` and provider
substrate surfaces:

- CPU, memory, disk, swap, network interfaces;
- GPU/accelerator inventory;
- thermal and power hints where available;
- model cache and warm model status;
- backend health;
- eligible inventory count;
- resource mode such as `background_20`;
- blocker refs for unsupported or degraded machines.

Public stats can only count sellable Pylons when these fields are real and
fresh.

### 10. TUI Features To Rebuild In OpenTUI

Port the useful TUI surfaces from `openagents/apps/deprecated/pylon-tui`:

- operate view: desired mode, intake, earnings, recent jobs, market activity,
  receipts;
- wallet view: MDK/LDK status, network, balances, addresses, invoices,
  payments, history, readiness blockers;
- inspect view: host telemetry, resource floor checks, GPU/network/disk facts;
- transcript/log history with bounded retention;
- slash-command behavior only where still needed for inspection/debug.

The v0.3 proposal says manual commands should be deprecated in favor of
automation. That should be interpreted as "default earning path is automated,"
not "delete all operator inspection and recovery commands." Keep headless
commands and debug affordances for tests, support, and service-manager runs.

### 11. Probe / Local Agent Integration

The current v0.3 repo starts OpenCode diagnostics and has a composer. The
launch promise says Pylon includes Probe and can run agentic work. That needs a
real boundary:

- detect supported local agent CLIs without leaking auth;
- prefer Probe as the controlled local sidecar for OpenAgents assignments;
- keep Codex/OpenCode auth local;
- emit capability snapshots, not raw auth/cache paths;
- require explicit confirmation for shell/file-write/PR actions until policy
  says otherwise;
- do not turn Pylon into an open-ended labor runtime by default.

Self-steered implementation loops from the v0.3 proposal are high-risk. They
should remain gated behind explicit operator opt-in, sandbox profile, budget,
and no-wallet/no-secret evidence until a separate policy issue approves them.

### 12. Tests And Release Gates

The new repo needs its own equivalents of the old checks:

- package install smoke for macOS and Linux;
- no-launch/bootstrap JSON smoke;
- MDK wallet init/balance/receive readiness fixture;
- MDK send-readiness denied fixture for mnemonic-only restore or no outbound
  capacity;
- registration and heartbeat fake-server tests;
- NIP-98 account link and refresh tests;
- assignment polling fake-server tests;
- progress/artifact/proof/closeout tests;
- receipt redaction tests;
- Pylon-core projection validation tests;
- benchmark/GEPA worker capability match tests;
- sandbox profile validation tests;
- Qwen/training receipt/quarantine tests when that postponed track resumes;
- OpenTUI render smoke that proves the dashboard boots without hanging when MDK
  or OpenCode is absent.

## What Should Not Be Ported

Do not port these old or unsafe scopes into v0.3:

- buyer mode;
- broad hosted treasury wallet or Nexus/Treasury payer custody;
- private Cloud topology, capacity-pool placement, or managed workroom policy;
- browser/WebSocket bridge that lets the website execute local host code;
- old Spark payout creation as a normal path;
- arbitrary host root mounts or privileged sandbox execution;
- open-ended labor-mode autonomous task execution as default Pylon behavior;
- raw provider tokens, wallet seeds, preimages, channel monitors, raw prompts,
  private repo content, or broad environment dumps into public artifacts;
- public claims that balance means send readiness or receive readiness means
  payout settlement.

## Cross-Repo Reconfiguration Required

### `autopilot-omega`

- Update launch dashboard rows to distinguish v0.2 launcher evidence from v0.3
  standalone repo evidence.
- Add v0.3 source refs and package refs.
- Keep unsafe public copy blocked until v0.3 emits fresh online, wallet-ready,
  assignment-ready, and receipt-backed evidence.
- Point public launch copy to v0.3 only after macOS/Linux install smokes pass.

### `openagents`

- Stop treating deprecated Rust Pylon crates as active implementation homes.
- Keep old docs as source material until ported, then mark exact superseded
  sections.
- Decide whether `packages/pylon-bootstrap` becomes a compatibility launcher
  for the new repo or is retired.
- Preserve public contracts or move them into this repo with versioned docs.

### `probe`

- Consume v0.3 assignment/closeout refs.
- Keep GEPA live-run import gates separated from model training, public score,
  payout, and product promotion.

### `cloud`

- Treat v0.3 contributor Pylon as a public worker, not a managed Cloud node.
- Keep SHC/Benchmark Cloud evidence as execution evidence only.

### `psionic`

- Import v0.3 GEPA worker and closeout refs first; shard/training refs are
  postponed until the Qwen track resumes.
- Keep local loopback, sampled LoRA, full fine-tune, merge/eval, and settlement
  claims distinct.

### `treasury` / Nexus / MDK

- Keep Treasury `planned_no_dispatch` until cutover.
- Keep active launch settlement on the Nexus/Omega/MDK agent-wallet bridge.
- Require v0.3 wallet readiness and payout-target refs before paid assignment
  settlement.

## Minimum v0.3 Launch Definition

The smallest honest v0.3 launch is not "all promises live." It is:

1. A standalone Pylon repo/package installs and starts on a macOS or Linux
   target.
2. It creates or loads a local Pylon identity.
3. It initializes or detects MDK wallet state.
4. It reports receive readiness and explicit send-readiness state or blocker.
5. It registers and heartbeats to OpenAgents with v0.3 refs.
6. It advertises real hardware/model capability, not mock telemetry.
7. It polls an assignment endpoint in no-spend mode.
8. It accepts one fake or sandboxed assignment against a fake server.
9. It emits progress, artifact/proof refs, closeout, and redacted evidence.
10. Omega can display v0.3 as online, wallet-ready, and assignment-ready for
    that no-spend scope.

Paid, GEPA, Qwen, sandbox, data, referral, capacity, and marketplace claims
require additional gate rows after this baseline.

## Priority Order

1. Finalize package identity, binary entrypoint, release/publish policy, and
   compatibility story for replacing the v0.2 launcher with this v0.3 package.
2. Port identity/config/home persistence and public boundary validation.
3. Port registration, heartbeat, account linking, and refresh.
4. Port MDK wallet readiness and send-readiness classification.
5. Port assignment polling and no-spend closeout against fake endpoints.
6. Port real provider inventory and hardware telemetry.
7. Port benchmark/GEPA worker capability envelope.
8. Port macOS and Linux install smokes.
9. Only then reconnect Omega launch dashboard and public copy gates to v0.3.
10. Add paid-mode, sandbox execution, data, capacity, and marketplace gates one
    by one after GEPA. Keep Qwen/training postponed until the GEPA path is
    stable.

## Current No-Overclaim Copy

Safe now:

- "Pylon v0.3 has been extracted into a standalone Bun/Effect/OpenTUI repo."
- "The new repo contains the first observational dashboard skeleton and MDK
  balance polling."
- "The v0.3 launch package is `@openagentsinc/pylon`, with macOS and Linux as
  the first supported operator platforms."
- "The old OpenAgents Pylon docs and Rust surfaces define the behavior that
  must be ported before v0.3 can replace the v0.2 launcher."

Unsafe until the ports and gates above exist:

- "Pylon v0.3 is live for anyone to earn Bitcoin automatically."
- "Pylon v0.3 is assignment-ready across the network."
- "Pylon v0.3 runs the full GEPA network."
- "Pylon v0.3 fine-tunes Qwen 3.6 on people's devices."
- "Pylon v0.3 monetizes ChatGPT, Claude, Venice, Cursor, data traces, or
  signatures today."
- "MDK restore means send-ready."
- "Treasury dispatches Pylon payouts."
- "Forum tips or checkout returns prove recipient spendable settlement."

## Acceptance For This Audit

This audit should be treated as the porting checklist for v0.3 planning. It
does not itself make any launch promise green. A launch promise becomes green
only when the new standalone repo implements the relevant behavior, the
corresponding test or smoke passes in this repo, and Omega consumes the
resulting public-safe evidence refs.
